<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">
    <CheckBoxPreference android:title="Enable sensors"
        android:defaultValue="false"
        android:summary="Enable sensor access to apps when ForceDoze is active (service restart required)"
        android:key="enableSensors" />
    <CheckBoxPreference android:title="Turn off while charging"
        android:defaultValue="true"
        android:summary="Turn off ForceDoze when device is plugged in to a charger (service restart required)"
        android:key="disableWhenCharging" />
    <Preference android:title="Reset ForceDoze"
        android:key="resetForceDoze"
        android:summary="Reset ForceDoze to the default state"/>
</PreferenceScreen>